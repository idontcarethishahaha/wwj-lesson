# 项目介绍

在这个项目中，我主要负责了四个核心微服务的后端开发：首先是**RMS用户微服务**，实现了完整的用户与权限管理体系，包含用户账号的增删改查、双渠道登录、密码与手机号管理、头像上传、数据统计，以及角色、菜单的配置与权限控制；其次是**CMS课程微服务**，搭建了从课程分类、课程发布、季次集次管理，到视频上传、弹幕管理、评论与举报处理的全流程课程内容管理能力；然后是**OMS订单微服务**，实现了购物车管理、订单创建与统计、订单明细管理，同时对接支付渠道完成预支付、回调处理、订单状态查询，并通过MQ消息实现了订单业务的异步解耦；最后是**SMS营销微服务**，覆盖了通知、新闻、轮播图等基础触达内容管理，同时开发了优惠券发放与口令查询、秒杀活动预热与高并发秒杀下单等营销功能，为整个平台的用户运营与转化提供了支撑。

我负责开发了 RMS 用户微服务的权限与用户管理模块，主要分为用户、角色和菜单三个核心接口。用户接口实现了基础的增删改查，同时支持密码重置与修改、头像上传、手机号换绑、账号与手机号双登录，还能进行用户数据统计和报表导出；角色接口提供了角色的基础增删改查，以及用户角色的查询与修改；菜单接口则负责菜单的增删改查，还有角色菜单的查询与配置，整体实现了一套完整的 RBAC 权限控制能力。

我负责开发了 CMS 课程微服务，围绕课程内容管理搭建了完整的业务模块：包含类别、课程、季次、集次、评论和举报六大模块。其中课程模块实现了基础增删改查、课程封面和摘要上传、课程列表搜索；集次模块支持视频封面和视频文件上传、数据报表下载以及弹幕列表查询；评论模块支持单条和批量删除用户评论；同时所有模块都实现了基础的增删改查能力，覆盖了课程从分类管理、内容发布、视频管理到用户互动与违规处理的全流程闭环。

我负责开发了 OMS 订单微服务，核心围绕电商订单流程搭建了完整的业务链路：包含购物车、订单、订单明细三大基础模块，实现了增删改查、购物车清空、订单数据统计和明细报表下载等功能；同时完成了前台下单的核心业务，包括预支付订单创建、支付二维码生成、支付回调处理和订单状态查询；还通过 MQ 消息处理实现了订单业务的异步解耦与可靠通知，保障了下单流程的稳定性和扩展性。

我负责开发了 SMS 营销微服务，搭建了从内容触达到营销转化的完整业务模块：包含通知、新闻、横幅等基础内容模块，实现了增删改查、置顶展示和轮播图上传；同时开发了优惠券模块，支持口令查询与发放管理；核心的秒杀模块则实现了秒杀活动预热、当日秒杀查询、秒杀课程下单及秒杀明细管理，保障了营销活动的基础触达与高并发场景下的秒杀业务闭环。



## 技术栈

# 项目技术栈总结（SpringCloud Alibaba 版，面试直接背）
我这个项目采用**SpringCloud Alibaba 微服务架构**搭建，核心技术栈以 SpringBoot 为基础，整合 Alibaba 一站式微服务组件实现服务治理与高可用。
服务注册与发现使用**Nacos**，同时通过 Nacos 配置中心统一管理各环境配置文件，实现配置动态刷新；服务间通信采用**OpenFeign**完成远程接口调用；针对高并发秒杀、流量突增场景，集成**Sentinel**实现接口限流、熔断降级，保证核心服务稳定性；使用**Seata**处理分布式事务，确保订单、库存、课程等跨服务数据一致性；异步解耦与消息通知基于**RocketMQ**实现，用于订单流转、秒杀下单、状态同步等业务场景；缓存与分布式锁采用**Redis**，配合 MySQL 完成数据持久化，整体架构具备服务可治理、高可用、可扩展的特点，完美支撑了平台用户、课程、订单、营销四大核心模块的业务落地。

---

## 精简口语版（30秒说完）
我这个项目用的是**SpringCloud Alibaba 微服务架构**，以 SpringBoot 为开发基础。用**Nacos**做服务注册发现和配置中心，**OpenFeign**做服务远程调用，**Sentinel**做限流熔断降级，**Seata**保证分布式事务，**RocketMQ**做异步消息解耦，配合 Redis 缓存和 MySQL 数据库，搭建了一套稳定、高可用的微服务体系，支撑了用户、课程、订单、营销核心业务的正常运行。



## 一、秒杀模块（高并发场景，面试必加分）
### 面试介绍版（可直接背）
在营销微服务的秒杀模块中，我重点解决了高并发场景下的**超卖、性能瓶颈和数据一致性**问题。业务上实现了秒杀活动预热、当日秒杀查询、课程秒杀下单和秒杀明细管理；技术上，我通过Redis缓存提前预热库存，用分布式锁+乐观锁防止超卖，同时将下单操作异步化到MQ中，前端配合静态化页面和限流，大幅降低了DB的直接压力，保证了秒杀高峰期的系统稳定性。

---

### 高频追问 & 标准答案（帮你提前准备）
| 面试官问题             | 你可以这样回答                                               |
| :--------------------- | :----------------------------------------------------------- |
| 秒杀怎么防止超卖？     | 我采用了Redis预减库存 + 数据库乐观锁的双重方案：先在Redis中扣减库存，库存不足直接拦截请求；请求进入DB层时，通过`update set stock = stock - 1 where id = ? and stock > 0`的乐观锁SQL，避免并发更新导致的超卖问题。 |
| 秒杀为什么要用MQ？     | 因为秒杀请求量巨大，直接同步写数据库会压垮DB。用MQ可以削峰填谷，把下单请求异步化，先返回“排队中”，再由后端消费消息处理订单，保证核心链路不被冲垮。 |
| 怎么防止用户重复下单？ | 用Redis的`SETNX`给用户ID+秒杀ID加分布式锁，或者给用户打上秒杀成功标记，限制同一用户对同一商品只能下一次单。 |

---

## 二、权限模块（基础但高频，体现设计能力）
### 面试介绍版（可直接背）
在用户微服务的权限模块中，我基于RBAC模型设计了用户-角色-菜单的权限体系。实现了用户账号全生命周期管理，同时通过自定义注解+AOP切面的方式，在接口层实现了细粒度的权限校验，避免了硬编码权限判断；另外还实现了用户角色和菜单的动态配置，保证了权限变更无需修改代码，提升了系统的可维护性。

---

### 高频追问 & 标准答案
| 面试官问题                   | 你可以这样回答                                               |
| :--------------------------- | :----------------------------------------------------------- |
| RBAC模型的核心是什么？       | 核心是用户-角色-权限的解耦：用户不直接关联权限，而是关联角色；角色再关联菜单/接口权限，这样修改权限只需要调整角色配置，不用改每个用户的权限。 |
| 你是怎么实现接口权限校验的？ | 我定义了一个`@RequiresPermission`注解，标注在需要权限的接口上；然后通过AOP切面，在接口调用前拦截请求，从当前登录用户中获取其角色对应的权限标识，和注解里的权限标识对比，不匹配就直接返回无权限。 |
| 权限数据怎么缓存？           | 为了避免每次请求都查DB，我把用户的权限列表缓存到Redis中，用户登录或权限变更时刷新缓存，既保证了性能，也避免了缓存和DB数据不一致的问题。 |

我帮你从这四个微服务里，提炼出**面试时最加分、也最容易被追问的4个核心难点**，每个都包含「问题场景+你的解决思路+技术亮点」，你可以直接用到项目介绍里：

---

### 难点1：秒杀场景下的超卖与高并发性能瓶颈
**场景**：秒杀活动期间，大量用户同时抢购课程，容易出现库存超卖、数据库压力过大、响应超时的问题。
**解决思路**：
- 用Redis提前预热库存，将大部分请求拦截在缓存层，避免直接打DB；
- 用`update ... where stock > 0`的乐观锁+Redis分布式锁双重校验，防止超卖；
- 引入MQ异步处理下单请求，实现削峰填谷，前端返回“排队中”，后端慢慢消费；
- 用页面静态化+接口限流，减少无效请求进入后端系统。
**亮点**：从“请求拦截-库存校验-异步处理”全链路解决了秒杀场景的并发问题，保证了系统稳定性。

---

### 难点2：文件上传与大视频传输的稳定性问题
**场景**：课程封面、集次视频、用户头像等文件上传，大文件传输易中断、服务器存储压力大、用户体验差。
**解决思路**：
- 采用分片上传+断点续传，将大文件拆分成小块上传，失败后从断点继续上传；
- 对接对象存储服务，避免直接将文件存在应用服务器，减轻服务压力；
- 对上传文件做格式、大小校验，避免恶意文件上传；
- 视频上传后异步转码，生成不同清晰度的视频流，适配不同网络环境。
**亮点**：解决了大文件上传的稳定性问题，同时实现了存储与业务解耦，提升了系统扩展性。

---

### 难点3：支付回调的幂等性与订单状态一致性问题
**场景**：支付回调可能重复调用、网络延迟导致订单状态更新不及时，容易出现重复扣款、订单状态不一致的问题。
**解决思路**：
- 用订单号+支付流水号作为唯一键，在数据库和Redis中做幂等性校验，同一回调只处理一次；
- 设计订单状态机，定义“待支付-支付中-已支付-支付失败”等状态流转规则，避免非法状态变更；
- 用定时任务轮询未支付订单，结合支付渠道接口查询状态，兜底处理回调丢失的情况；
- 支付成功后通过MQ异步通知其他服务（如积分、优惠券），避免主流程阻塞。
**亮点**：保证了交易链路的安全性和数据一致性，解决了支付场景的核心痛点。

---

### 难点4：权限体系的细粒度控制与动态配置问题
**场景**：不同角色用户（管理员、老师、学生）的菜单、接口权限不同，权限硬编码扩展性差，修改权限需要重启服务。
**解决思路**：
- 基于RBAC模型设计用户-角色-菜单的权限体系，实现权限与用户解耦；
- 通过自定义注解+AOP切面实现接口级别的权限校验，避免硬编码权限判断；
- 将用户权限列表缓存到Redis，用户登录或权限变更时刷新缓存，提升校验性能；
- 实现菜单与角色的动态配置，修改权限只需在后台配置，无需修改代码或重启服务。
**亮点**：实现了细粒度、可配置、高性能的权限控制，提升了系统的可维护性和扩展性。



# 面试回答版（流畅、完整、不啰嗦）
在我的项目里，**读多写少、高频访问、变化不频繁**的数据基本都会放到 Redis 做缓存，主要包括这几类：

第一类是**用户权限相关数据**，比如用户的角色列表、菜单权限、接口权限集合，我会以用户 ID 作为 key，把权限列表存在 Redis 的 Hash 或 Set 结构里，用户登录后直接从缓存获取，不用每次都查数据库，大幅提升接口鉴权速度。

第二类是**课程模块的热点数据**，比如课程详情、课程分类、轮播图、推荐课程列表、弹幕数据等，这些访问量很大但更新不频繁，我用 String 或 Hash 存储课程信息，设置合理的过期时间；课程列表则用 ZSet 做排序展示，提升首页加载速度。

第三类是**订单与购物车数据**，购物车直接用 Hash 存储，key 是用户 ID，field 是课程 ID，value 存数量和状态；未支付订单的状态、订单简要信息也会缓存，减少重复查询。

第四类是**秒杀和营销相关数据**，秒杀库存我用 Redis 原子操作来扣减，保证高并发下不超卖；优惠券信息、活动状态、用户秒杀资格标记也存在 Redis，用 String 和 Set 结构快速判断，避免直接打库。

整体上，我通过 Redis 分担数据库读压力，用 String、Hash、Set、ZSet 等结构适配不同业务场景，并结合过期策略和主动更新保证缓存与数据库一致性，提升系统并发能力和响应速度。

---

# 如果你想要更短、更口语的版本（30秒）
我项目里主要把**用户权限、课程详情、分类列表、首页轮播、购物车、秒杀库存、优惠券**这些高频读、低频改的数据放到 Redis。
用户权限用 Hash 存，课程信息用 String，购物车用 Hash，秒杀库存用 String 做原子扣减，列表类用 ZSet 排序，既减轻 DB 压力，也保证高并发场景下接口响应更快、系统更稳定。



